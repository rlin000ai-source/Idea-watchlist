# Upstash Redis — auto-populated when you provision Upstash Redis via the Vercel Marketplace
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Single shared secret for both viewing and editing the watchlist
# Generate with: openssl rand -base64 32
WATCHLIST_TOKEN=
